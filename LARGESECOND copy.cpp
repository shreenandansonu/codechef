#include <bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int n;
        cin >> n;
        int arr[n],maxnum=INT_MIN,mindiff=INT_MAX;
        for (int i = 0; i < n; i++)
        {
            cin >> arr[i];
            maxnum=max(maxnum,arr[i]);
            mindiff=min(mindiff,)
        }

    }
    return 0;
}